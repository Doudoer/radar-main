server {
    listen 80;
    listen [::]:80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Serve static files from the root
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests
    location /api/ {
        proxy_pass http://backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Proxy storage requests
    location /order_attachments/ {
        proxy_pass http://backend:3001/order_attachments/;
    }

    location /payment_receipts/ {
        proxy_pass http://backend:3001/payment_receipts/;
    }

    location /delivery_proofs/ {
        proxy_pass http://backend:3001/delivery_proofs/;
    }

    location /health {
        access_log off;
        return 200 'OK';
    }
}
